{
  "version": 2,
  "buildCommand": "npm install && CI=false npm run build",
  "outputDirectory": "build",
  "framework": null,
  "root": "frontend",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
